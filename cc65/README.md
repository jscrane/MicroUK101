# cc65 for MicroUK101

Standard I/O and `conio` implementations for MicroUK101.

`read.s` and `write.s` are based on the KIM-1 implementations.

`conio.s` uses ASCII escape sequences for a VT102 terminal.

`crt0.s` is from the `none` target.
